wireplumber.components = [
    {
        name="90-steam-recording.lua"
        type=script/lua
        provides=custom.steam-recording
    }
]

wireplumber.profiles = {
    main = {
        custom.steam-recording = required
    }
}
